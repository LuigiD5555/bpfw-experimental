#!/usr/bin/env sh
set -eu

if [ -z "${BPFW_MANIFEST_HMAC_KEY:-}" ] && [ -f ".bpfw/local_hmac_key" ]; then
  BPFW_MANIFEST_HMAC_KEY="$(cat .bpfw/local_hmac_key)"
  export BPFW_MANIFEST_HMAC_KEY
fi

bpfw verify --ci
